import { Card } from "@mui/material";
import React, { FunctionComponent } from "react";

const Properties: FunctionComponent = () => {
  return <Card>properties</Card>;
};

export default Properties;
